import { Credentials as GoogleToken } from 'google-auth-library';
import { AgendaItem } from './interfaces/agenda.interface';
import { Config } from './config';
export interface GoogleCredentials {
    installed: {
        client_secret: string;
        client_id: string;
    };
}
export declare function readEvents(startTime: Date, endTime: Date, calendarId: string, config: Pick<Config, 'google_api_credentials' | 'google_api_token'>): void;
export declare function removeEvents(startTime: Date, endTime: Date, calendarId: string, config: Pick<Config, 'google_api_credentials' | 'google_api_token'>): void;
export declare function pushToCalendar(events: AgendaItem[], calendarId: string, config: Pick<Config, 'google_api_credentials' | 'google_api_token'>): void;
export declare function getGoogleAccessToken(credentials: GoogleCredentials): Promise<GoogleToken>;
