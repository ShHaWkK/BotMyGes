{
    "name": "set-up",
    "type": 1,
    "description": "Command for set the schedul channel, the reminder channel and the error channel",
    "options": [
        {
          "type": 7,
          "name": "schedule-channel",
          "description": "ID of the channel for the schedule",
          "required": true
        },
        {
          "type": 7,
          "name": "reminder-channel",
          "description": "ID of the channel for all the reminders",
          "required": true
        },
        {
          "type": 7,
          "name": "error-channel",
          "description": "ID of the channel for potential errors",
          "required": true
        }
    ]
}